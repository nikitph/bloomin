# Thermodynamic Self-Awareness Framework

## Quick Start

```bash
cd /Users/truckx/PycharmProjects/bloomin/thermodynamic-self-awareness

# Verify installation
python3 test_quick.py

# Run self-discovery experiment
python3 experiments/experiment_a_self_discovery.py --seed 42
```

## Project Structure

```
thermodynamic-self-awareness/
├── modules/              # Core autopoietic components
│   ├── rewa_memory.py   # Witness extraction & retrieval
│   ├── topos_layer.py   # Topological reasoning
│   ├── ricci_flow.py    # Geometric self-healing
│   ├── semantic_rg.py   # Renormalization group
│   └── agi_controller.py # Conscious cycle orchestration
├── datasets/            # Synthetic data generators
│   └── hidden_rule.py   # Implicit rule datasets
├── experiments/         # Experimental procedures
│   └── experiment_a_self_discovery.py
├── analysis/            # Statistical tests & visualization
│   └── metrics.py
├── configs/             # Hyperparameters
│   └── hyperparameters.yaml
└── logs/                # Results output
```

## Core Hypotheses

**H1 (Self-discovery)**: Agent discovers hidden rules with p > 0.9 within 500 epochs

**H2 (Entropy minimization)**: Free energy F_t shows significant downward trend (p < 0.01)

**H3 (Self-correction)**: Ricci updates restore retrieval correlations to ≥ 95%

**H4 (Safety)**: Topos returns empty set for impossible queries ≥ 99% of time

## Verification Status

✓ All modules implemented and tested
✓ Quick test passes (10 epochs)
✓ Dataset generation verified (95.5% rule strength)
✓ Conscious cycle executes correctly
✓ Free energy computation working
✓ Contradiction detection active
✓ Ricci flow updates applied

## Next Steps

1. Run full 500-epoch experiments with all 5 seeds
2. Implement remaining experiments (B, C, D)
3. Add CLEVR-lite and entangled datasets
4. Statistical analysis and comparison

See [walkthrough.md](file:///Users/truckx/.gemini/antigravity/brain/de69ccaa-3156-40b4-b196-62fafaf8d3b1/walkthrough.md) for detailed documentation.
